export const categoryState = (state) => state.categoryState;
export const postState = (state) => state.postState;
export const detailState = (state) => state.detailState;
export const authState = (state) => state.authState;
